import 'dart:ui';
// import 'package:sky_engine/ui/text.dart'; 

// import 'package:flutter/services.dart';

// import 'part_middle.dart';

// import 'part_lib.dart';

class SomeType {
  get foo => 'some export';

  // SomeType({this.textAlign = FontStyle.italic});

  // final FontStyle textAlign;

  // SomeType({this.color = const Color.fromARGB(50, 50, 50, 0)});

  // final Color color;
  
  // SomeType({this.cap = TextCapitalization.sentences});

  // final TextCapitalization cap;

  // SomeType({this.someEnum = SomeEnum.one});

  // final SomeEnum someEnum;
}
